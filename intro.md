# 임진강김치 PLC 데이터수집 디지털 트윈

## 1. 한 줄 소개

㈜임진강김치 스마트공장(선도형 스마트공장 구축지원사업)에서 도입하는 **센서·설비 → PLC 제어반 → Edge Collector → MES** 데이터 수집 경로를 소프트웨어로 똑같이 재현(디지털 트윈)하고, 그 수집 현황을 **실시간 대시보드**로 보여주는 프로토타입이다.

## 2. 근거 자료

이 프로젝트는 로뎀솔루션이 작성한 임진강김치 분석·설계 산출물을 기준으로 한다. 설비 모델, 수량, 통신 방식, 수집 항목, 테이블 구조는 아래 문서에서 가져왔다.

| 산출물 | 경로 | 이 프로젝트에서 쓴 내용 |
|---|---|---|
| SF-AD1 현행업무분석서 | `01 Analyze Agent/outputs/20260810-211328-all/` | 공정 흐름, 현행 문제, 도입 H/W 13종 |
| SF-AD2 요구사항정의서 | 〃 | 공정별 수집·알람 요구사항 (MES-AD2-071·072 등) |
| SF-TD2 아키텍처설계서 | `02 Design Agent/outputs/20260811-231318-all/` | 시스템 계층, 데이터 집계 포인트 9종, 네트워크 |
| SF-TD4 프로그램설계서 | 〃 | MES-TD4-065 설비·센서 데이터 인터페이스 흐름 |
| SF-TD5 데이터베이스설계서 | 〃 | `IF_SENSOR_RAW`, `BAS_EQUIP` 및 공정별 수집 테이블 |

두 폴더의 파일은 `00 PM Agent/outputs/납품_임진강김치_20260912/`에 있는 납품본과 같다.

## 3. 대상 공장

| 항목 | 내용 |
|---|---|
| 회사 | ㈜임진강김치 (경기도 연천군 연천읍 은통산단1길 14) |
| 제품 | 율무 총각김치, 율무 포기김치, 배추·총각·열무김치 등 |
| 인증 | HACCP 적용업소, ISO 22000 |
| 현행 | 공정·CCP 기록 수기, 설비 데이터는 설비 표시부를 보고 옮겨 적음 |
| 목표 | 제어시스템·센서 신규 도입, 첫 MES 구축 (스마트공장 수준 기초 → 중간1) |

공정 흐름:

```
입고/보관 → 절단/전처리 → 세척/절임 → 세척/선별 → 탈수 → 혼합(버무림) → 금속검출 → 포장/출고 → 냉장·숙성
```

## 4. 재현하는 수집 구조

```
[현장 센서·설비]                        [PLC 제어반]               [Edge]               [MES]
염도센서 2식          ─ RS-485 ─┐
온습도센서 10식       ─ RS-485 ─┼──▶ PLC Master (XBC-DN32H) ─┐
온도조절기 4식        ─ RS-485 ─┤        ▲ Ethernet          │
금속검출기            ─ RS-485 ─┤   PLC Slave  (XBC-DN32H) ─┼─Ethernet─▶ Edge Collector ─MQTT/HTTP─▶ 수집 API
소독수 공급장치       ─ Ethernet(XBL-EMTA600) ─┘   (절단/전처리)│           (Mini PC T1 X300)       → IF_SENSOR_RAW
아이스박스자동포장기 KF100 ──────── Ethernet · OPC-UA ─────────┘           프로토콜 표준화         → 공정별 테이블
중량 저울 · 충진기 · 속넣기기계 ─── (통신 방식 미확정) ─────────┘           로컬 버퍼링·재전송
```

설비로 제어 명령은 보내지 않고 **데이터를 읽기만** 한다. 사업계획서 1.4에서 전 공정의 실시간 제어가 X이기 때문이다.

## 5. 대시보드에서 보는 것

| 화면 | 내용 |
|---|---|
| 수집 토폴로지 | 센서 → PLC Master/Slave → Edge → MES 경로를 그림으로 보여주고, 구간별 상태를 색으로 표시 |
| 공정 현황 | 공정 흐름도 위에 센서·설비 상태와 대표값 (염도, 소독수 ppm, 냉장고 온도 등) |
| 수집 현황 | 설비별 폴링 주기, 성공률, 응답시간, 마지막 수신 시각, 오류 건수 |
| PLC 메모리 | Master/Slave의 D영역 레지스터 값과 태그 매핑 |
| Edge 버퍼 | MES 전송 대기 건수, 재전송 건수(`RESEND_YN`), 최근 전송 결과 |
| 알람 | 소독수 10ppm, 절임 염도, 냉장 온도, 금속검출 NG, 통신 끊김 |
| 시뮬레이션 제어 | 센서 고장, RS-485 단선, PLC–Edge 끊김, MES 회선 장애 주입 |

## 6. 문서 구성

| 문서 | 내용 |
|---|---|
| `intro.md` | 이 문서. 프로젝트 소개 |
| `problem.md` | 풀려는 문제, 목표, 범위, 확정이 필요한 사항 |
| `spec.md` | 설비·프로토콜·PLC 메모리 맵·Edge·수집 테이블·화면·API·수용 기준 |

## 7. 용어

| 용어 | 뜻 |
|---|---|
| 디지털 트윈 | 실제 센서·PLC·Edge와 같은 동작을 하는 소프트웨어 복제본 |
| PLC 제어반 Master/Slave | LS산전 XBC-DN32H 2식. Master는 세척·절임·냉장 쪽, Slave는 절단/전처리 구역 |
| Edge Collector | Mini PC에서 도는 수집 프로그램. 프로토콜을 표준 메시지로 바꾸고, 회선 장애 때 버퍼링한다 |
| `IF_SENSOR_RAW` | MES 설비센서수집원장 테이블. 모든 수집값이 먼저 여기 쌓인 뒤 공정별 테이블로 나뉜다 |
| RS-485 / Modbus RTU | 센서 여러 대를 한 선에 물리는 직렬 통신과 그 위의 표준 프로토콜 |
| OPC-UA | 설비 데이터를 이름 붙은 노드로 주고받는 산업용 표준 (자동포장기가 지원) |
| CCP | HACCP 중요관리점. 소독수 농도, 버무림, 금속검출, 냉장 온도 등 |
