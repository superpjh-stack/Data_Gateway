# 문제 정의 — 임진강김치 설비·센서 데이터 수집

## 1. 배경

㈜임진강김치는 매출이 '22년 282억원에서 '24년 609억원으로 늘었다. 생산 능력도 1만kg에서 3만kg으로 키워야 한다. 그런데 공정·품질 데이터는 카카오톡, 팩스, 엑셀, 수기에 흩어져 있다. 설비 데이터는 설비 표시부를 사람이 보고 옮겨 적는다(SF-AD1).

이번 사업에서 PLC 제어반 2식, 센서 17식, Edge Collector, 아이스박스자동포장기를 새로 들여와 설비 데이터를 MES로 자동 수집한다(SF-TD2). 제어시스템·센서가 전혀 없던 공장에 수집 경로를 처음 까는 것이다. 그래서 **장비가 들어오기 전에** 수집 구조를 검증하고, 들어온 뒤에는 **수집이 제대로 되는지 보여줄** 도구가 필요하다.

## 2. 현재 문제

### 2.1 공장의 문제 (SF-AD1에서)

| # | 문제 | 관련 수집 데이터 |
|---|---|---|
| F1 | 소독수 농도·세척 시간이 기록되지 않는다 | 소독수 농도(ppm, 기준 10ppm), 접촉시간 |
| F2 | 절임통 8기의 염도·시간을 수동 관리해 동시 진행 현황을 알기 어렵다 | 염도(9·12·13%), 경과시간 |
| F3 | 냉장·냉동고 온도를 담당자가 직접 보고 비정기적으로 기록한다 | 온도, 습도, 설정온도 |
| F4 | 금속검출 결과를 수기로만 남겨 전수 이력이 없다 | OK/NG, 검사 수량, 불합격 수량 |
| F5 | 포장·테이핑이 수작업이라 출하 병목이고 실적 집계가 늦다 | 포장 완료 수량, 가동/정지 |
| F6 | 설비 가동 데이터가 없어 가동률을 계산할 수 없다 | 가동/정지 상태 |

### 2.2 수집 시스템을 구축하면서 생길 문제

| # | 문제 | 영향 |
|---|---|---|
| P1 | 통신 방식이 섞여 있다 (RS-485, Ethernet, OPC-UA, 미확정 설비) | 설비마다 따로 개발·시험해야 한다 |
| P2 | 경로가 4단계(센서 → PLC → Edge → MES)다 | 값이 안 들어올 때 어느 구간이 문제인지 찾기 어렵다 |
| P3 | 장비 반입 전에는 시험할 대상이 없다 | 태그 매핑·수집 로직 오류가 설치 후에야 드러난다 |
| P4 | 설비 코드와 PLC 태그가 1:1로 맞아야 한다 (`BAS_EQUIP.PLC_TAG`) | 주소가 틀리면 다른 설비 값이 기록된다 |
| P5 | Edge 로컬 버퍼링·재전송을 검증하기 어렵다 | 회선 장애 때 HACCP 기록이 빠질 수 있다 |
| P6 | 수집 주기, 버퍼 보존 기간, 일부 설비 통신 방식이 설계서에서도 미확정이다 | 착수 시 정할 값을 미리 시험해 볼 수단이 없다 |

## 3. 핵심 질문

1. 설계서의 센서·설비 구성과 통신 방식대로 수집 경로를 **실제 장비 없이** 재현할 수 있는가?
2. 운영자가 한 화면에서 **"어느 센서가, 어느 구간에서, 왜"** 수집이 안 되는지 알 수 있는가?
3. 회선이 끊겨도 Edge 버퍼링으로 **수집값이 빠짐없이** MES에 도착하는가?
4. 장비가 들어오면 가상 설비를 **하나씩 실물로 바꿔도** 같은 대시보드와 로직으로 동작하는가?
5. 미확정 값(수집 주기, 염도센서–절임통 매핑 등)을 바꿔 가며 **착수 전에 결정 근거**를 만들 수 있는가?

## 4. 목표

### 4.1 프로토타입 목표

| # | 목표 | 측정 기준 |
|---|---|---|
| G1 | 설계서의 수집 대상을 모두 가상으로 구현 | 염도 2, 온습도 10, 온도조절기 4, 소독수 1, 금속검출기 1, 자동포장기 1, 미확정 설비 3(가정) = 22개 |
| G2 | PLC Master/Slave 트윈이 RS-485 센서를 폴링해 D영역에 기록 | 정상 조건 수집 성공률 99% 이상 |
| G3 | Edge Collector 트윈이 PLC·OPC-UA·저울 값을 표준 메시지로 바꿔 MES로 전송 | 메시지가 `IF_SENSOR_RAW` 형식과 일치 |
| G4 | 구간별 장애 판별 | 센서·RS-485 버스·PLC–Edge·Edge–MES 중 끊긴 구간을 대시보드에 표시 |
| G5 | 버퍼링·재전송 | MES 회선을 10분 끊었다 복구해도 누락 0건, 복구분은 `RESEND_YN=Y` |
| G6 | 기준 이탈 알람 | 소독수 10ppm, 염도, 냉장 온도, 금속검출 NG 알람 발생·해제 기록 |
| G7 | 실시간 대시보드 | 센서 값 변화가 화면에 2초 이내 반영 |

### 4.2 이후 목표 (범위 밖, 방향만)

- 현장 실사 후 가정값을 실제 사양으로 교체하고 실장비와 `hybrid` 모드로 연결
- 실제 임진강김치 MES 수집 API와 연결
- 단위·통합 테스트(SF-CD1, SF-TI1)의 설비 연동 시험 도구로 활용

## 5. 범위

### 포함

- 가상 센서·설비 22개 (설계서 19개 + 미확정 설비 가정 3개)
- PLC 제어반 Master/Slave 트윈: RS-485 폴링, D영역 메모리, Edge 쪽 통신 제공
- Edge Collector 트윈: 프로토콜 변환, 노이즈 필터, 로컬 버퍼, 재전송
- MES 수집 API 스텁: `IF_SENSOR_RAW` 적재와 공정별 테이블 분배(수집 관련 테이블만)
- 수집 현황 대시보드, 알람, 고장·장애 주입

### 제외

- 설비 제어(쓰기 명령). 사업계획서 1.4에 따라 전 공정 실시간 제어 X
- MES 업무 기능(수주, 생산계획, 재고, 검색 Agent 등)
- 실제 PLC 래더 프로그램 작성, 현장 배선
- AWS 배포, 인증(OAuth2), ERP 연계

## 6. 이해관계자

| 역할 | 관심사 |
|---|---|
| 임진강김치 생산·품질(HACCP) 담당 | 소독수·염도·냉장 온도·금속검출 기록이 빠짐없이 남는가 |
| 임진강김치 설비 담당 | 어느 센서·설비의 통신이 왜 끊겼는가 |
| 로뎀솔루션 PM | 착수 시 확정할 값의 근거, 설치 후 검증 증적 |
| PLC·Edge 개발자 | 태그 매핑과 수집 로직을 장비 없이 먼저 시험 |

## 7. 가정과 제약

- **설계서에서 확정된 것은 그대로 따른다.** PLC 기종(XBC-DN32H), 센서 모델·수량, 통신 방식, 테이블 구조가 여기에 해당한다.
- **설계서에서 미확정인 것은 가정값을 쓰고 `spec.md`에 `가정` 표시를 한다.**
  - 수집 주기, 버퍼 보존 기간
  - 금속검출기 대수 (사업계획서에는 없고, 선행 현행업무분석서에만 2대로 기재)
  - 중량 저울·충진기·속넣기기계의 통신 방식
  - 염도센서 2식과 절임통 8기의 매핑
  - 센서별 Modbus 레지스터 주소, PLC D영역 주소
  - 냉장·냉동 온도 기준값, 염도 허용 범위
- 직렬 통신(RS-485, RS-232)은 가상 시리얼 포트 또는 TCP로 감싼 시리얼로 대신한다.
- 한 PC에서 모든 구성 요소가 돌아가야 한다.

## 8. 확정이 필요한 사항 (착수·현장 실사)

설계서에 "착수 시 확정" 또는 "담당자 확인 필요"로 남아 있고, 트윈에 직접 영향을 주는 항목이다.

| # | 항목 | 근거 | 트윈의 가정 |
|---|---|---|---|
| Q1 | 설비별 수집 주기 | TD5 `IF_SENSOR_RAW` 비고, TD4-065 | 센서 5~10 s, 금속검출기 1 s |
| Q2 | Edge 버퍼 보존 기간 | TD2 4.2 | 72시간 |
| Q3 | 염도센서 2식 ↔ 절임통 8기 매핑 | AD2, TD5 `SLT_SALINITY_LOG` | 센서 1식당 절임통 4기, 측정 대상을 순환 전환 |
| Q4 | 금속검출기 대수·통신 지원 여부 | TD2 데이터 집계 포인트 5 | 1대, RS-485 Modbus RTU |
| Q5 | 중량 저울 통신 방식 | TD2 집계 포인트 7 | RS-232 ASCII → Edge |
| Q6 | 충진기·속넣기기계 통신 방식 | TD2 집계 포인트 8 | Modbus TCP → Edge |
| Q7 | 냉장고 대수, 온도조절기 4식 배치 | AD1 | 숙성·원품·탈수 냉장고 + 냉동고 1 |
| Q8 | 냉장·냉동 온도 기준, 염도 허용 범위 | `BAS_CCP_STD`에서 관리하나 값 미기재 | 냉장 -1~5℃, 냉동 -18℃ 이하, 염도 목표 ±1.0%p |
| Q9 | 설비망·사무망 분리 | TD2 | 트윈에서는 구분하지 않음 |
| Q10 | 온습도센서 10식의 Master/Slave 배분 | TD2 (Slave = 절단/전처리 구역) | 입고 3·전처리 3 → Slave, 나머지 4 → Master |

## 9. 성공의 모습

대시보드를 열면 수집 토폴로지에 센서 22개, PLC 2식, Edge, MES가 모두 초록색으로 떠 있다. 시뮬레이션 패널에서 "Master RS-485 버스 단선"을 켜면 그 버스의 센서만 빨갛게 바뀌고, Slave 쪽 전처리 센서는 계속 초록이다. 다음으로 "MES 회선 장애"를 켜면 Edge 버퍼 건수가 올라간다. 끄면 버퍼가 비워지고, `IF_SENSOR_RAW`에 `RESEND_YN=Y` 행이 빠짐없이 들어온다. 소독수 농도를 9ppm으로 떨어뜨리면 세척 공정 카드에 알람이 뜨고, `WSH_SANITIZER_LOG`에 `ALARM_YN=Y`가 기록된다.
